# Руководство по Русской Локализации

## ✅ Что было сделано

### 1. Supabase Интеграция
- ✅ Удален режим демо
- ✅ Полная интеграция с Supabase
- ✅ Поддержка анонимного сохранения результатов
- ✅ Загрузка текстов из Supabase с fallback на локальные

### 2. Русская Локализация
- ✅ Установлен next-intl
- ✅ Создан файл переводов `messages/ru.json`
- ✅ Настроен middleware для принудительного использования русского языка
- ✅ Обновлен layout для поддержки i18n

### 3. Обновленные Компоненты
- ✅ `app/page.tsx` - Главная страница
- ✅ `app/trainer/page.tsx` - Тренажер чтения
- ✅ `lib/supabase.ts` - Клиент Supabase
- ✅ `app/api/saveResult/route.ts` - API для сохранения

## 📋 Что нужно обновить вручную

### Компоненты, требующие обновления:

1. **components/trainer/Recorder.tsx**
   - Заменить все английские тексты на `useTranslations()`
   - Использовать ключи из `messages/ru.json`

2. **components/trainer/ScoreCard.tsx**
   - Добавить `useTranslations()`
   - Перевести все тексты

3. **components/trainer/TranscriptDiff.tsx**
   - Добавить `useTranslations()`
   - Перевести легенду и заголовки

4. **components/game/BadgeDisplay.tsx**
   - Использовать переводы для значков

5. **components/game/Mascot.tsx**
   - Использовать переводы для сообщений

6. **app/profile/page.tsx**
   - Полностью перевести на русский

7. **app/auth/login/page.tsx**
   - Полностью перевести на русский

8. **app/auth/register/page.tsx**
   - Полностью перевести на русский

## 🔧 Инструкции по настройке

### 1. Установи зависимости
```bash
npm install
```

### 2. Создай .env.local
Скопируй `.env.local.template` в `.env.local` и заполни:
```
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### 3. Настрой Supabase
1. Создай проект на https://supabase.com
2. Выполни SQL из `supabase/schema.sql` в SQL Editor
3. Скопируй URL и anon key в `.env.local`

### 4. Запусти проект
```bash
npm run dev
```

## 📝 Пример использования переводов

```tsx
'use client';

import { useTranslations } from 'next-intl';

export function MyComponent() {
  const t = useTranslations();
  
  return (
    <div>
      <h1>{t('home.title')}</h1>
      <p>{t('home.subtitle')}</p>
    </div>
  );
}
```

## 🎯 Следующие шаги

1. Обнови все компоненты для использования переводов
2. Протестируй сохранение результатов в Supabase
3. Убедись, что тексты загружаются из базы данных
4. Проверь работу анонимного сохранения

